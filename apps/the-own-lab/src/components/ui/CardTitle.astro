---
import { cn } from "ui";

interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}

const { class: className, as = "div", ...rest } = Astro.props;
const Tag = as;
---

<Tag data-slot="card-title" class={cn("leading-none font-semibold", className)} {...rest}>
  <slot />
</Tag>
