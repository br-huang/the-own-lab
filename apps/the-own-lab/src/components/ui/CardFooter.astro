---
import { cn } from "ui";

interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}

const { class: className, as = "div", ...rest } = Astro.props;
const Tag = as;
---

<Tag data-slot="card-footer" class={cn("flex items-center p-6 pt-0", className)} {...rest}>
  <slot />
</Tag>
