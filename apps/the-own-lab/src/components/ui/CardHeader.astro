---
import { cn } from "ui";

interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}

const { class: className, as = "div", ...rest } = Astro.props;
const Tag = as;
---

<Tag data-slot="card-header" class={cn("flex flex-col gap-1.5 p-6", className)} {...rest}>
  <slot />
</Tag>
