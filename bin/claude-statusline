#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

if [[ -f "$PROJECT_ROOT/dist/main.js" ]]; then
  exec node "$PROJECT_ROOT/dist/main.js"
fi

exec node --import tsx "$PROJECT_ROOT/src/main.ts"
